/**
 * 数字に変換する
 *
 * @version 0.5.0
 * @since 0.5.0
 * @param str 対象の文字列
 * @param negative 負の値を許可してマイナスをつけるかどうか
 * @param floatingPoint 小数を許可してドットをつけるかどうか
 */
export default function (str: string, negative?: boolean, floatingPoint?: boolean): string;
